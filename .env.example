# XMaps Backend API URL
# This should point to your backend server that handles X API and Grok API calls
VITE_API_BASE_URL=http://localhost:3001

# Connection type for live updates (optional)
# Options: 'sse' (Server-Sent Events) or 'websocket'
# Default: 'sse'
# VITE_CONNECTION_TYPE=sse
